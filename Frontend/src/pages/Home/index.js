import React from 'react';
import {Container} from './styles';

import DeviceCard from '../../components/DeviceCard';

const Home = () => {
  return (
    <Container>
      <DeviceCard />
    </Container>
  );
};

export default Home;
